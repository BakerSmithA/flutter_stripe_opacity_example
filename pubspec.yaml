name: flutter_stripe_opacity_example
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter

  flutter_stripe: ^11.0.0
  flutter_stripe_web: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
